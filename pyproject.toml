[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "research-agent"
version = "0.1.0"
description = "AI-powered news research and political bias analysis for YouTube creators"
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
authors = [
    { name = "Your Name", email = "you@example.com" }
]
keywords = ["ai", "agents", "crewai", "news", "research", "bias", "youtube"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

dependencies = [
    # Agent Framework
    "crewai>=0.95.0",
    "crewai-tools>=0.14.0",
    "litellm>=1.50.0",
    
    # News & Web
    "feedparser>=6.0.0",
    "ddgs>=6.0.0",
    "trafilatura>=1.12.0",
    "newspaper4k>=0.9.0",
    
    # Keyword Analysis
    "yake>=0.4.8",
    
    # YouTube
    "yt-dlp>=2024.0.0",
    
    # Database
    "sqlalchemy>=2.0.0",
    "alembic>=1.14.0",
    
    # API
    "fastapi>=0.115.0",
    "uvicorn>=0.32.0",
    "pydantic>=2.10.0",
    "pydantic-settings>=2.6.0",
    
    # CLI
    "click>=8.1.0",
    "rich>=13.9.0",
    
    # Utilities
    "python-dotenv>=1.0.0",
    "httpx>=0.28.0",
    "pyyaml>=6.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.24.0",
    "pytest-cov>=4.1.0",
    "ruff>=0.8.0",
    "mypy>=1.13.0",
    "pre-commit>=4.0.0",
]

ml = [
    # Optional: Better keyword extraction (requires more dependencies)
    "keybert>=0.8.0",
    "sentence-transformers>=3.0.0",
]

[project.scripts]
research-agent = "src.cli.main:cli"

[project.urls]
Homepage = "https://github.com/yourusername/research-agent"
Documentation = "https://github.com/yourusername/research-agent#readme"
Repository = "https://github.com/yourusername/research-agent"
Issues = "https://github.com/yourusername/research-agent/issues"

[tool.hatch.build.targets.wheel]
packages = ["src"]

# Ruff Configuration
[tool.ruff]
target-version = "py311"
line-length = 88
select = [
    "E",      # pycodestyle errors
    "W",      # pycodestyle warnings
    "F",      # Pyflakes
    "I",      # isort
    "B",      # flake8-bugbear
    "C4",     # flake8-comprehensions
    "UP",     # pyupgrade
    "ARG",    # flake8-unused-arguments
    "SIM",    # flake8-simplify
]
ignore = [
    "E501",   # line too long (handled by formatter)
    "B008",   # function call in argument defaults
]

[tool.ruff.isort]
known-first-party = ["src"]

[tool.ruff.per-file-ignores]
"tests/*" = ["ARG"]

# Mypy Configuration
[tool.mypy]
python_version = "3.11"
strict = true
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = ["crewai.*", "ddgs.*", "trafilatura.*", "newspaper.*", "yake.*", "feedparser.*"]
ignore_missing_imports = true

# Pytest Configuration
[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "-v --tb=short"
