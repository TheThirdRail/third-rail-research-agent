# Comey Case Regression Fixture

Sanitized expectations derived from the local dev-branch report context. These
fixtures encode behavior, not exact prose:

- one renderer-owned Source Matrix
- one renderer-owned citation block
- missing `right_side` coverage fails by default
- observable visual evidence stays separate from interpretation
- contextual-only coverage is rejected by default
